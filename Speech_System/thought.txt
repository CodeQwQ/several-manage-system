整体的思路：
    1.对象有这个系统和选手，所以两个类就可以
    2.每次的分组，晋级的分组都可以用数组来装，名字要设好
    3.




遇到的坑：
    1.#pragma once只能在.h文件里用，而且要记得用，不然一堆redifinition的报错
    2.resize那行代码有问题，不知道是不是分配失败造成的
    3.static 函数类外定义不用加static关键字
    4.函数里面用到的函数要放在这个函数的上面，比如bool cmp, 给for_each的print函数